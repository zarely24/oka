import { sqliteTable, text, integer, index, primaryKey } from 'drizzle-orm/sqlite-core';
export const bookings = sqliteTable('bookings', { id: text('id').primaryKey(), suite: text('suite').notNull().default('portside'), name: text('name').notNull(), email: text('email').notNull(), phone: text('phone').notNull(), arrival: text('arrival').notNull(), departure: text('departure').notNull(), guests: integer('guests').notNull(), note: text('note').notNull(), status: text('status').notNull().default('pending'), created: text('created').notNull() });
// One row per occupied night per suite. booking_id is a bookings.id or the literal 'blocked' for owner blocks.
export const nights = sqliteTable('nights', { suite: text('suite').notNull().default('portside'), day: text('day').notNull(), bookingId: text('booking_id').notNull() }, t => [primaryKey({ columns: [t.suite, t.day] }), index('idx_nights_booking').on(t.bookingId)]);
